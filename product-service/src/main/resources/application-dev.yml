# 商品服务 —— 本地开发配置（指导书 4.3.3）
# 本地数据库与注册中心
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/wims_product?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka
