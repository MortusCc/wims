# 保证 shell 脚本与 Dockerfile 以 LF 存储(CRLF 会导致 Linux 上 bash/build 报错)
*.sh text eol=lf
Dockerfile* text eol=lf
# k8s/Spring 配置 YAML 同样强制 LF,避免 git 行尾告警与 Linux 端混乱
*.yaml text eol=lf
*.yml text eol=lf
# Jenkins 流水线脚本强制 LF
Jenkinsfile text eol=lf
